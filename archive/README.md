# Archive

The archive preserves original projects as historical artifacts.

Do not clean or rewrite code directly in `archive/originals/` unless a file is
unsafe to keep. Build cleaned teaching versions in `frontend/`, `backend/`,
`lessons/`, `demos/`, or `exercises/` instead.
