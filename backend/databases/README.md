# Databases

## Purpose

This chapter teaches tables, keys, relationships, queries, and small interfaces
over data.

## Teaching Progression

```text
tables -> keys -> relationships -> queries -> small interface
```

## Imported Projects

- `Zoo-Database` -> `zoo-database`
- `Zoo-Interface` -> `zoo-interface`

## Possible Future Lessons

- Tables and columns
- Primary and foreign keys
- Simple SELECT queries
- Joins and relationships
- Connecting a small interface to data
- Separating data model code from interface code

## Cleanup TODOs

- Import original source when available.
- Keep generated database files out of Git unless intentionally tiny fixtures.
- Add schema diagrams only when they clarify relationships.
