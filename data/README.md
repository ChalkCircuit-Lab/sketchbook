# Data

This folder contains the small JSON catalogs used by the static index.

- `projects.json` describes each original or cleaned project.
- `chapters.json` describes the concept order for teaching.
- `tags.json` keeps shared labels consistent.

Keep the data hand-editable and valid JSON.
