# Bootstrap

## Purpose

This chapter teaches how a component library can speed up layout while still
requiring clear HTML structure and readable intent.

## Teaching Progression

```text
container -> grid -> utilities -> cards -> navbar -> responsive page
```

## Imported Projects

- `bootstrap_demo_5` -> `bootstrap-demo-5`
- `bootstrap_demo_8` -> `bootstrap-demo-8`
- `bootstrap_avatar_cards_12` -> `bootstrap-avatar-cards-12`

## Possible Future Lessons

- Reading Bootstrap markup
- Grid rows and columns
- Cards and repeated content
- Navigation bars
- Replacing custom CSS with utilities carefully

## Cleanup TODOs

- Import originals when source is available.
- Record whether history was preserved.
- Remove accidental generated files from cleaned versions.
- Keep original rough versions in `archive/originals/`.
